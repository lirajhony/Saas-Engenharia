# ADR (Architecture Decision Records)

Toda decisão estrutural (ex.: trocar DB, mudar auth, padronizar multi-tenant, alterar estratégia de IA)
deve gerar um ADR com contexto, decisão e consequências.

Use o template em `docs/10-adr-template.md`.
